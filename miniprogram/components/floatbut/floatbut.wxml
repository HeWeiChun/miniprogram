<view class='float_box'>
    <view>
      <view class='float_main' catchtap='release_function'  wx:if="{{release_sign == true}}">
         <image class='float_img' src='../images/release2.svg' />
      </view>
       <view class='float_main' catchtap='itemclick' wx:if="{{backTop == true}}">
         <image class='float_img' src='../images/top.png' />
      </view> 
    </view>
</view>

<mp-actionSheet bindactiontap="btnClick" show="{{showActionsheet}}" actions="{{groups}}" title="我的发布">
</mp-actionSheet>
